= link_to 'New Enquiry', new_admin_pet_path, class: 'btn btn-primary pull-right'
%h1 Listing Enquiries
%table.table
  %tr
    %th From
    %th To
    %th Created
    %th Actions

  - @enquiries.each do |enquiry|
    %tr
      %td= enquiry.user.name
      %td= enquiry.homestay.user.name
      %td= "#{time_ago_in_words enquiry.created_at} ago"
      %td
        =link_to 'Show', admin_enquiry_path(enquiry)
        =link_to 'Edit', edit_admin_enquiry_path(enquiry)
        =link_to 'Destroy', admin_enquiry_path(enquiry), :method => :delete, :data => { :confirm => 'Are you sure?' }
