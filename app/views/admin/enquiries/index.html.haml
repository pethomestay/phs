= link_to 'New Enquiry', new_admin_pet_path, class: 'btn btn-primary pull-right top-margin'
%h1 Listing Enquiries
%table.table.tablesorter
  %thead
    %tr
      %th From
      %th To
      %th Created
      %th Actions
  %tbody
    - @enquiries.each do |enquiry|
      %tr
        %td= link_to enquiry.user.name, admin_enquiry_path(enquiry)
        %td= link_to enquiry.homestay.user.name, admin_enquiry_path(enquiry)
        %td= enquiry.created_at.to_formatted_s(:year_month_day)
        %td
          =link_to 'Edit', edit_admin_enquiry_path(enquiry)
          =link_to 'Destroy', admin_enquiry_path(enquiry), :method => :delete, :data => { :confirm => 'Are you sure?' }

:javascript
  $(document).ready(function(){
    $('table.tablesorter').tablesorter({headers:{3:{sorter:false}}});
  });
