= simple_form_for @account, html: { class: 'form-horizontal' } do |f|
  = render :partial => "account_fields", :locals => {f: f}

  .form-actions
    = f.submit 'Save', id: 'account-save-btn', class: 'btn btn-primary'
