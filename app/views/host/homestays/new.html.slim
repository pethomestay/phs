= render layout: 'layouts/user' do
  .homestay-panel
    h1 Create Your PetHomeStay Listing
    = simple_form_for @homestay, html: { multipart: true }, wrapper: :bootstrap3 do |f|
      = render 'form_fields', f: f
      .form-actions
        = f.submit 'Create my listing', class: 'btn btn-primary'
