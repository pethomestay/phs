- flash.each do |type, message|
  :coffee
    $.growl
      message: "#{message}",
        type: "#{flash_class_for type}"
        delay: 10000
        mouse_over: 'pause'
        offset:
          x: 0
          y: 0
        animate:
          enter: 'animated fadeInRightBig'
          exit: 'animated fadeOutRightBig'

- if defined? resource
  - resource.errors.full_messages.each do |type, message|
    :coffee
      $.growl
        message: "#{message}",
          type: "#{flash_class_for type}"
          delay: 10000
          mouse_over: 'pause'
          offset:
            x: 0
            y: 0
          animate:
            enter: 'animated fadeInRightBig'
            exit: 'animated fadeOutRightBig'
