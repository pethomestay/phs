:css
  #infoa { margin-top: -5px;}
.span12
  .page-header
    .pull-left
      %h1 My Calendar

    .pull-right
      = link_to 'Back to My Account', guest_path, class: 'btn'
    .clear


  .alert.fade.in.success.hide#calendar_update_alert
    Calendar updated successfully

  .well.clearfix
    %p
      This Calendar controls availability for your HomeStay.
      It will decide if your HomeStay is displayed when a Guest searches a date range on our homepage.

    %p Simply click on the individual days to rotate them from being Available to Unavailable, then click Update My Calendar.
    %p If you are booked as a Host, Stays are automatically added to your Calendar and show as Unavailable.
    %p To completely remove your HomeStay from Guest searches, click on 'Edit', and deactivate your HomeStay listing. You can reactivate it at any time!

  #update-calendar.clearfix
    = link_to 'Update My Calendar', "javascript:" , class: 'btn btn-primary pull-right', id: "update-calendar-btn"
  .clear

  #calendar

  %table.table.tablesorter
    %thead
      %tr
        %th Title
        %th Suburb
        %th Created
        %th Active?
        %th Approved?
        %th Actions
    %tbody
      %tr
        %td= link_to @homestay.title, homestay_path(@homestay)
        %td= @homestay.address_suburb
        %td= @homestay.created_at.to_formatted_s(:year_month_day)
        %td{:id => "is_active_" + @homestay.id.to_s}
          =@homestay.active? ? "Active" : "Inactive"
        %td{:id => "is_locked_" + @homestay.id.to_s}
          =@homestay.locked? ? "Pending Approval" : "Approved"
        %td
          =link_to 'Edit', edit_homestay_path(@homestay)
          -if !@homestay.locked
            =link_to @homestay.active? ? 'Deactivate' : 'Activate', homestay_activate_path(@homestay), id: 'active_toggle_' + @homestay.id.to_s, remote: true, :method=>:post
        %td
          = image_tag 'info-image.png', id: "infoa",class: "icon", rel: "tooltip", :'data-placement'=>"top", :'title'=>'You can choose to temporarily take a break from PetHomeStay. If you select "deactivate" your listing will not take new bookings and you will not appear in searches.'
